{
  "name": "gemini-cli-bridge",
  "version": "0.1.0",
  "description": "Agent-friendly CLI bridge for Gemini CLI web search, web fetch, and image analysis.",
  "type": "module",
  "bin": {
    "gemini-cli-bridge": "./bin/gemini-cli-bridge.ts",
    "gcb": "./bin/gemini-cli-bridge.ts"
  },
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "start": "bun run bin/gemini-cli-bridge.ts"
  },
  "engines": {
    "bun": ">=1"
  },
  "license": "MIT",
  "files": [
    "bin",
    "src",
    "skills",
    "README.md"
  ],
  "devDependencies": {
    "bun-types": "^1",
    "typescript": "^6.0.3"
  }
}
